import { WhatsappExistDirective } from './whatsapp-exist.directive';

describe('WhatsappExistDirective', () => {
  it('should create an instance', () => {
    const directive = new WhatsappExistDirective();
    expect(directive).toBeTruthy();
  });
});
