import { PhoneValidatorsDirective } from './phone-validators.directive';

describe('PhoneValidatorsDirective', () => {
  it('should create an instance', () => {
    const directive = new PhoneValidatorsDirective();
    expect(directive).toBeTruthy();
  });
});
