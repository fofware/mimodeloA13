import { EmailcheckDirective } from './emailcheck.directive';

describe('EmailcheckDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailcheckDirective();
    expect(directive).toBeTruthy();
  });
});
