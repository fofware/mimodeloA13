import { ProdNameFilterPipe } from './prod-name-filter.pipe';

describe('ProdNameFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ProdNameFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
