import { ExtraDataFilterPipe } from './extra-data-filter.pipe';

describe('ExtraDataFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtraDataFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
